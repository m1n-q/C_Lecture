#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	

	//int input, sum = 0 ;

	//int status;

	//printf("Enter an Integer (q to quit) : ");
	//status = scanf("%d", &input);	// return : 입력받은 items 수
	//
	//
	//
	//while (status == 1)
	//{

	//	sum += input;
	//	printf("Enter an Integer (q to quit) : ");
	//	status = scanf("%d", &input);
	//	
	//	
	//}

	
	// scanf : %d 에 문자가 들어오면 입력을 받지도 않는당.
	//         즉, return 값 (items) 가 0이 된다.

	return 0;
}
